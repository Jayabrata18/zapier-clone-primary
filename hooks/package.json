{
  "name": "hooks",
  "version": "1.0.0",
  "main": "app.ts",
  "scripts": {
    "start": "tsc -b && node src/app.ts",
    "build": "tsc",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts",
    "precommit": "lint-staged",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JOY",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "@types/express": "^4.17.21",
    "express": "^4.19.2",
    "prisma": "^5.18.0",
    "typescript": "^5.5.4"
  }
}
